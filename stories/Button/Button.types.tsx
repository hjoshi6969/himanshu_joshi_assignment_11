import Button from "./Button";Button.propTypes = {
    label: PropTypes.string.isRequired, 
    backgroundColor: PropTypes.string.isRequired, 
    fontColor: PropTypes.string.isRequired,
};
import PropTypes from "prop-types"

